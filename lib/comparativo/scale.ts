/** Factor único cm → px para pantalla (misma escala relativa entre obras). */
export const COMPARATIVO_PX_PER_CM = 2.85

/** Fuentes Cloudinary al exportar PNG (≈3.5× pantalla, antes de pixelRatio del canvas). */
export const COMPARATIVO_EXPORT_PX_PER_CM = 10

/** Resolución embebida en PDF comparativo (puntos @72dpi + imagen a esta DPI). */
export const COMPARATIVO_PDF_DPI = 200
