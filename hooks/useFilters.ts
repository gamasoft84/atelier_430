"use client"

// Implementación completa en Fase 4
export function useFilters() {
  return {
    filters: {},
    setFilter: (_key: string, _value: unknown) => {},
    clearFilters: () => {},
    activeCount: 0,
  }
}
